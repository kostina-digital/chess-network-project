node_modules

# clerk configuration (can include secrets)
/.clerk/

# local env (secrets)
.env.local
.env.*.local
.env
.next

# User-uploaded blog images (keep directory via .gitkeep)
public/uploads/posts/*
!public/uploads/posts/.gitkeep

# Prisma client (schema output); run prisma generate or npm install
src/generated/prisma/
.vercel
